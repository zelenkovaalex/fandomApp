<%= form_with model: @fandom, url: admin_fandoms_path, local: true do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.label :description %>
  <%= form.text_area :description %>

  <%= form.submit 'Create Fandom' %>
<% end %>