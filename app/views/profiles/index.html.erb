<p style="color: green"><%= notice %></p>

<% content_for :title, "Profiles" %>

<h1>Profiles</h1>

<div id="profiles">
  <% @profiles.take(6).each do |profile| %>
    <%= render profile %>
    <p>
      <%#= link_to "Show this profile", profile %> 
    </p>
  <% end %>
</div>
